`review-code` skill을 실행한다.

code-reviewer-agent를 단독으로 호출하여 backend/, frontend/, tests/와 api-contract.json의 교차 검증을 수행하고 review.md를 생성한다. STAGE 2가 완료된 뒤 재검토·디버깅 목적으로 단독 실행할 때 사용한다. ARGUMENTS에 session-id를 입력한다.

ARGUMENTS: $ARGUMENTS
