`export` skill을 실행한다.

생성된 앱 산출물(backend/, frontend/, tests/, docs/, README.md)을 지정한 target-dir로 내보낸다. ARGUMENTS에 session-id와 target-dir 절대 경로를 순서대로 입력한다. 예: 20260601-todo-app /home/user/projects/todo-app

ARGUMENTS: $ARGUMENTS
